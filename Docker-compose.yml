version: '1-0'
services:
  web_dev:
    ports:
      - "80:80"
    volumes:
      - /mnt/apache/dev/:/usr/local/apache2/htdocs/
    build: ./dev/
  
  web_prod:
    ports:
      - "8081:80"
    volumes:
      - /mnt/apache/prod/:/usr/local/apache2/htdocs/
    build: ./prod/
